<!DOCTYPE html>
<html>
  <head>
    <title>RailsReactImportmap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-slate-50 p-4 top-0 sticky">
      Navigation
    </nav>
    <div class="flex">
      <aside class="p-4 bg-slate-100 w-1/6">
        Sidebar
      </aside>
      <main class="bg-slate-200 w-5/6 p-4">
        Main
        <% (1..100).each do |i| %>
          <p><%= i %></p>
        <% end %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
