<!-- Updates table entry -->
<%= turbo_stream.replace dom_id(@person), partial: 'person', locals: { person: @person } %>
<!-- Closes the slide-over -->
<%= turbo_stream.remove 'person_form' %>
<%= turbo_stream.update 'flash-messages', partial: 'layouts/flash_wrapper' %>