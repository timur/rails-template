<div class="grid grid-cols-8 gap-4" data-controller="sortable" data-sortable-url-value="<%= examples_patch_reorder_path %>">
  <% @pictures.each do |picture| %>
    <%= image_tag @service.thumb_url(picture.image), id: picture.id, class: "item object-cover object-center w-36 h-36 rounded-lg cursor-grab" %>
  <% end %>
</div>
