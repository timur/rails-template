<%= form_with model: picture, id: dom_id(picture, :form), data: { controller: "form" } do |form| %>
  <%= form.check_box :selected, id: dom_id(picture, :selected), data: { action: "form#submit" } %>
<% end %> 