<%= form_with model: picture, id: dom_id(picture, :form), class: "absolute z-50 left-2 top-2", data: { controller: "form" } do |form| %>
  <%= form.check_box :selected, id: dom_id(picture, :selected), data: { action: "click->form#checkbox" } %>
<% end %>